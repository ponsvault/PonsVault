export interface SeatSeries {
  seriesId: number;
  creator: string;
  token: string;
  collection: string;
  amm: string;
  activation: string;
  booster: string;
  loan: string;
  name: string;
  symbol: string;
  maxSupply: string;
  createdAt: number;
}
